
# StackHost configuration file

# Version: 2.0.0

user:
  name: "AdnanEternal"
  github: "AdnanEternal"

runtime:
  # Supported runtimes: python, node, go, rust, ruby, java, php, dotnet, elixir, perl, swift
  image: python:3.14-slim # e.g., python:3.11-slim or node:22

commands:
  package: '' # apt packages, space-separated (must be approved)
  build:
    - "pip install splusthon"
  start: "python3 sorush_boy.py"

repository:
  branch: 'main'
  auto_deploy: true

env:
  # - KEY:VALUE
  # - KEY:VALUE
